# 住得满 AI 新媒体 Skills 与提示词体系

规则版本：`2026.07.24-v9`

默认文本模型：`qwen3.7-max-2026-06-08`。服务端和设置页均使用该固定快照模型 ID，以保证生产结果和回归测试更容易复现。

## 1. 这不是单一长提示词

系统采用“阶段路由 + 短运行提示词 + 本地 RAG + 确定性校验”的四层结构。当前只有选题、内容两个阶段调用模型；发布补全和数据复盘已定义独立 Skill 契约，但 MVP 由前端与领域规则确定性执行，尚未开放对应模型 API。

1. 阶段路由先把任务限定为选题、内容、发布或复盘之一。
2. 后端只读取该阶段 Skills 的 `references/runtime-prompt.md`；完整 `SKILL.md` 只用于治理、审阅和规则中心展示。
3. 本地 RAG 按当前酒店、当前题目和素材分类召回 Top-K 事实、历史与图片候选，未命中资料不进入模型上下文。
4. 模型返回 JSON 后，服务端再次执行长度、枚举、图片白名单、事实引用、风险和质量门禁。

因此内部开发版“AI 规则中心”同时展示当前模型实际装载的指令，以及发布、复盘两个待生产适配阶段的治理契约；正式用户版不展示这些研发信息。

## 2. 八个阶段 Skills

| Skill | 职责 | 主要触发 |
|---|---|---|
| `hotel-media-orchestrator` | 总编排、账号隔离、状态机、降级和执行轨迹 | 所有任务 |
| `hotel-fact-grounding` | OTA 与商家知识库证据包、冲突和易变信息 | 选题、写作、发布前 |
| `hotel-topic-strategy` | 532、账号阶段、历史表现、素材库存和选题评分 | 今日推荐、换题、复盘回写 |
| `hotel-content-production` | 标题、正文、标签、钩子、封面字、CTA 和镜头结构 | 生成、重写 |
| `hotel-visual-director` | 当前酒店真实图片、封面、顺序、镜头和补拍任务 | 内容配图、发布前 |
| `hotel-compliance-review` | 事实、广告、平台、品牌、长度、素材六层审核 | 输出后、编辑后、发布前 |
| `douyin-publish-completion` | 创作者中心字段映射、权限、定时和适配器状态 | 确认发布 |
| `hotel-performance-loop` | 曝光互动转化、素材消耗、异常和下一轮实验 | 日周月复盘 |

技能目录位于 [`skills/`](./skills/)。每个技能包含：

- `SKILL.md`：核心流程和硬规则。
- `agents/openai.yaml`：技能名称、描述和默认调用提示。
- `references/runtime-prompt.md`：当前阶段实际装入模型的短指令。
- `references/*.md`：证据政策、选题框架、平台手册、视觉标准、审核矩阵、发布字段或指标定义。

## 3. 任务模式与技能装载

| 阶段 / API | 技能链 | 当前执行方式 |
|---|---|---|
| `topic-recommendation` / `/api/ai/topics` | 总编排 → 事实 → 选题 | 模型 + 服务端规范化 |
| `content-generation` / `/api/ai/content` | 总编排 → 事实 → 内容 → 视觉 → 合规 | 模型 + 服务端门禁 |
| `publish-preparation` | 总编排 → 事实 → 合规 → 发布补全 | 本地确定性表单补全；模型契约预留 |
| `performance-analysis` | 总编排 → 数据复盘 | 本地指标聚合；模型契约预留 |

`daily`、`swap` 只映射到选题阶段；`focus-topic`、`regenerate` 只映射到内容阶段。旧的 `/api/ai/recommend` 一体化接口已停用，防止一次请求同时推荐选题和生成正文。

## 4. 模型输入契约

### 4.1 选题阶段

- 当前账号 ID、酒店 ID 与精简商家画像。
- Top 8 相关酒店事实。
- Top 8 历史内容及曝光、互动、类型。
- 最多 5 类素材库存聚合；不发送单张图片 ID。
- 已拒绝选题 ID、规则版本和本轮三个 Skill。

### 4.2 内容阶段

- 用户明确选择的 `focusTopic`。
- 前端将该选题规范化为持久化 `activeTopic`，完整保留选题 ID、原始标题、类型、目标、客群、内容角度、素材类别和事实引用。
- 草稿保存 `topicContext`，编辑页、重新生成、图片召回和发布记录始终沿用同一选题上下文；不得用生成后的作品标题反向替代原选题。
- 工作台按 `模型推荐 → 确认选题 → 内容生成 → 确认内容 → 发布补全` 计算真实状态，不使用写死的完成标记；每个阶段提供显式确认或跳转按钮。
- Top 10 与题目相关的酒店事实。
- Top 5 相似的已发布历史内容。
- Top 12 图片候选 ID、分类、标题、来源和使用次数。
- 规则版本和本轮五个 Skill。

### 4.3 明确排除

- 其他测试账号或其他酒店的任何数据。
- 当前查询未命中的事实和历史。
- 选题阶段的单图详情与完整素材库。
- 内容阶段的选题策略长规则、发布字段和复盘指标。
- 完整 `SKILL.md`、完整 OTA 原始文件和模型 API Key。

本地检索采用中文词项/双字词、字段优先级、素材分类加权、历史相似度与表现分组合排序。查询命中权重高于静态字段优先级，确保用户选定的主题主导召回。

## 5. 事实与证据规则

1. 只使用当前账号知识库和 OTA 快照明确存在的事实。
2. 每个对外宣称进入 `claimEvidence`，并映射到 `factReferences`。
3. POI 直线距离不得改写为步行或驾车时间。
4. 点评标签只能表述为部分住客反馈，不能成为酒店官方保证。
5. 模型从图片推断出的面积、景观、方向、楼层和设施不可对外使用。
6. 价格、房态、优惠、套餐、库存、活动和营业时间必须人工确认。
7. 证据冲突时优先商家人工确认，其次 OTA 酒店官方字段；无法判断则不写。
8. 任何跨账号事实或图片都属于高风险并阻断发布。

## 6. 选题推荐规则

### 6.1 532 是滚动结构

最近 10 条内容目标结构为：流量型 50%、垂类型 30%、营销型 20%。系统根据当前缺口提高对应类型权重，但不会在证据或素材不足时机械凑比例。

### 6.2 候选评分

每个候选总分 100：

- 事实支撑：25。
- 受众需求：20。
- 素材可执行：15。
- 差异化：15。
- 历史机会：15。
- 转化路径：10。

事实支撑低于 15 或素材可执行低于 8 的选题不得进入前三。

### 6.3 三条推荐必须真正不同

目标受众、核心利益、主要证据和素材分类四项中有三项相同即视为重复。`swap` 模式会把上一轮完整标题、受众、内容角度和素材分类写入 `rejectedTopics`，至少改变其中两项，不能只做标题同义改写。服务端会再次检查标题相同或互为包含关系；命中重复时自动要求模型重试一次。

每条推荐分成两层字段：

- 正式用户层：选题标题、推荐说明、适合人群、内容讲法和素材基础；必须是用户能直接理解的自然中文。
- 内部执行层：检索依据、事实引用、风险提示、执行轨迹和评分；只用于内部工程版、服务端校验和诊断。

正式用户层不得出现 `fact-1`、`materialInventory`、`rejectedTopics`、`history-*`、`evidenceId`、`factReferences`、RAG、Top-K、素材 ID 或检索过程。前端与服务端都必须过滤旧缓存或模型偶发返回的内部术语。

## 7. 内容生产规则

1. 标题最多 20 个中文字符；正文目标约 120 字，必须在 100-140 字之间；长度、历史相似度或换版差异不合格时最多执行三轮自动修复。
2. 一条内容只表达一个核心信息。
3. 正文按“场景或问题 → 真实利益点 → 轻行动引导”组织。
4. 标签 3-5 个，组合城市、住宿品类、场景和主题。
5. 同时输出封面字、钩子、CTA、评论引导、音乐氛围、图片计划和镜头顺序。
6. 不伪装住客，不编造第一人称入住经历，不制造虚假稀缺。
7. 避免“推开门就惊艳、正确打开方式、天花板、封神”等模板化 AI 表达。
8. 重写时必须改变钩子、受众、场景或节奏中的至少一项。

## 8. 视觉与图片规则

- 图片只能从当前账号有效素材路径中选择。
- 图文固定选择 5 张，即 1 张封面和 4 张内容图；同篇素材 ID 和文件内容不得重复，存在视觉组字段时至少覆盖 4 个视觉组。顺序为“封面主题 → 环境建立 → 核心证据 → 细节补充 → 位置承接”。
- 发布历史按酒店和平台分别计算：图片组合检查最近 10 篇，封面检查最近 8 篇；历史素材只降权、不永久禁用，但每篇进入发布确认前至少有 2 张未在该平台最近 10 篇出现。
- 与同平台任一近期单篇重合 4—5 张时阻断，重合 3 张时重选，重合 2 张时允许但封面必须不同；封面不得重复该平台最近 8 篇封面。
- 首图按 3:4 主体安全区优化；自动选图结果必须保留素材 ID、用途、裁切建议和选择依据。
- 每条内容独立绑定当前酒店的名称、OTA事实地址和经纬度；抖音门店 POI 只有适配器返回授权 ID 时才标记“已匹配”，否则显示“待抖音门店匹配”。
- “换一版”必须挂载上一版标题、正文、钩子、CTA和图片 ID，更换封面、至少替换 2 张图片、标题相似度低于 65%，并更换正文首句。
- 自动修复固定为换图、换叙事角度、换题三轮；仍无法通过时停止进入发布确认，并返回不超过 6 张的补拍清单。
- 标题与同平台最近 30 篇相似度达到 80%时重写；正文达到 85%时重写；正文达到 80%且与同一历史内容重合至少 2 张图时阻断。
- 首图应在 0.5 秒内说明主题，优先低使用次数、主体清晰的真实图片。
- 不把不同房型拼成一个房型介绍；无法确认时改写为“空间合集”。
- POI 图片不能证明酒店内部设施。
- AI 图只能作为明确标识的氛围示意，不能冒充房型、设施、景观或餐食。
- 没有合适素材时返回具体补拍任务，不使用无关图片凑数。

## 9. 六层审核与质量门禁

模型自检和服务端确定性检查共同执行：

1. 事实证据。
2. 易变信息。
3. 广告表达。
4. 平台安全。
5. 品牌一致。
6. 发布完整性。

质量分 100：事实 25、合规 20、策略 15、品牌 10、平台表达 15、素材 15。

逐项公式、扣分条件、发布门禁和示例见 [`CONTENT_QUALITY_SCORING.md`](./CONTENT_QUALITY_SCORING.md)。工程分用于判断内容完整性与风险，不代表抖音流量预测、法律结论或平台审核结果。

- 85-100：推荐发布。
- 70-84：可进入人工确认。
- 50-69：需要重写。
- 0-49：不可执行或存在严重问题。
- 任一高风险：无论分数多少，`publishGate=blocked`。

服务端不会直接相信模型自评分，而是根据实际字段、事实引用、风险扫描和素材计划重新计算。

## 10. 分阶段输出 JSON 契约

### 10.1 选题阶段

```json
{
  "strategySummary": "本次策略",
  "executionTrace": {
    "skills": ["hotel-media-orchestrator", "hotel-fact-grounding"],
    "decisions": ["关键决策与依据"],
    "evidenceGaps": ["缺失证据"],
    "manualConfirmations": ["人工确认项"]
  },
  "recommendations": [
    {
      "title": "选题",
      "type": "traffic",
      "objective": "曝光",
      "targetAudience": "目标人群和场景",
      "displayReason": "为什么今天值得做这个选题",
      "reason": "事实、数据和素材依据",
      "hook": "开头钩子",
      "contentAngle": "核心叙事角度",
      "materialCategory": "room",
      "materialReadiness": "现有实拍素材能支撑的画面",
      "materialPlan": ["画面需求"],
      "factReferences": ["地址｜OTA"],
      "riskHints": [],
      "score": 82
    }
  ]
}
```

`recommendations` 必须恰好三条，且不得输出 `content`。`displayReason`、`targetAudience`、`contentAngle`、`materialReadiness` 面向正式用户；`reason`、`factReferences` 和 `executionTrace` 为内部执行信息。

### 10.2 内容阶段

```json
{
  "executionTrace": {
    "skills": ["hotel-content-production", "hotel-visual-director"],
    "decisions": ["关键决策与依据"],
    "evidenceGaps": [],
    "manualConfirmations": []
  },
  "content": {
    "title": "20字内标题",
    "body": "100至140字正文，目标约120字",
    "tags": ["#大理民宿", "#大理旅行", "#住宿推荐"],
    "coverText": "12字内封面字",
    "hook": "一秒钩子",
    "cta": "轻量行动引导",
    "commentPrompt": "自然评论引导",
    "materialCategory": "room",
    "materialIds": ["只能来自本轮materialCandidates的ID"],
    "imagePlan": [{ "category": "room", "purpose": "首图", "selectionRule": "主体明确" }],
    "shotPlan": [{ "order": 1, "scene": "房间全景", "overlay": "封面短字" }],
    "musicMood": "自然松弛",
    "factReferences": ["酒店名称｜OTA"],
    "claimEvidence": [{ "claim": "文案中的事实", "evidence": "酒店名称｜OTA" }],
    "requiresConfirmation": false,
    "manualConfirmations": [],
    "selfReview": {
      "qualityScore": 86,
      "scoreBreakdown": {
        "grounding": 25,
        "compliance": 20,
        "strategy": 14,
        "brand": 9,
        "platform": 13,
        "material": 5
      },
      "riskLevel": "safe",
      "issues": [],
      "publishGate": "ready"
    }
  }
}
```

内容阶段不得输出 `recommendations`。正文不在 100-140 字内时服务端拒绝本次结果并自动要求模型重写一次，不通过截断残句伪造合格；服务端还会清理标签、限制字段枚举、过滤候选列表之外的图片 ID、重新计算质量分并执行风险扫描。

## 11. 多平台发布准备与补全规则

确认发布后，本地确定性流程复用同一酒店事实和五图，分别生成抖音、小红书和微信视频号版本。标题、正文和标签按平台习惯独立适配；抖音继续补全创作者中心字段。

没有实时活动、热点、音乐曲库或抖音门店接口结果时保持空值或仅给氛围建议，不虚构具体 ID。

当前 `demo` 适配器只写入本地发布记录。获得官方直发能力的平台才标记“已提交”；其他平台只生成发布包并标记“待客户端确认”。生产接入需保存平台任务 ID、错误码、重试状态和幂等键。

## 12. 数据复盘规则

复盘只统计当前账号和已发布内容。样本少于三条时只做描述，不形成稳定因果。每轮实验只改变一个主要变量，并记录假设、基线、目标指标、观察窗口与停止条件。当前由本地指标聚合执行，数据复盘 Skill 是后续模型化分析的受控契约。

系统把复盘结果回写到：选题权重、推荐钩子、禁用模式、素材优先级和补拍任务。

## 13. 正式用户版“我的”派生规则

“我的”不新增模型阶段，也不独立生成任务。内容助手状态必须由当前账号的既有内容工作流确定性派生，点击后返回首页、创作或发布补全的对应状态。

服务推荐只允许读取当前酒店的素材数量与分类覆盖、发布记录以及抖音账号、地点、商品和来客连接状态。每次最多推荐一项，输出用户可理解的推荐原因；推荐上下文不得反向注入选题或内容生成提示词。

用户可调整品牌语气、默认发布时间和提醒偏好。100-140 字、1 封面 + 4 内容图、事实约束、图片白名单和发布门禁属于系统规则，不得在正式用户版变成可关闭设置。

帮助和反馈携带当前酒店、页面、内容 ID 与工作流状态，方便定位问题；不得自动附带模型密钥、完整私信、联系方式明文或内部检索字段。

## 14. 代码位置

- 运行时技能编排：[src/skill-engine.js](./src/skill-engine.js)
- 本地 RAG 检索：[src/rag-engine.js](./src/rag-engine.js)
- 模型调用与服务端校验：[server.mjs](./server.mjs)
- 前端执行与规则中心：[src/app.js](./src/app.js)
- 全部技能包：[skills/](./skills/)
- 自动化测试：[tests/ai-server.test.js](./tests/ai-server.test.js)
