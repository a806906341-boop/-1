# GitHub 更新包使用说明

更新日期：2026-07-27

## 本次更新重点

- 当前产品基线升级为 PRD v1.4。
- 现役入口固定为 `/` 内部工程版和 `/user-a.html` 正式用户版。
- 正式用户版一级导航调整为：首页、创作、内容、互动、我的。
- 内容数据并入“内容－数据”，服务归入“我的”，桌面端与移动端保持一致。
- 内容生成固定使用 1 张封面 + 4 张内容图。
- 三个测试账号的 924 张照片保存在 `assets/hotels/`，可离线检索和自动选图。
- 选题和内容生成接入分阶段 Skills、酒店事实 RAG 与质量门禁。
- 本次为双端更新包，不包含内容运营中台入口和中台专属代码。

## 上传方法

不要把压缩包本身作为项目源码提交。先解压，然后把解压后的全部内容覆盖到 GitHub 仓库工作目录，再通过 Git 提交和推送。

推荐在仓库目录执行：

```bash
npm test
npm run serve
```

本地入口：

- 内部开发版：`http://localhost:5173/`
- 正式用户版：`http://localhost:5173/user-a.html`

## 未包含内容

- `.env.local` 及任何真实 API Key。
- `大理100家酒店调研交付_20260716/` 的约 2.3GB 原始下载缓存。
- 历史研发交付目录、旧 ZIP 和旧 Git bundle。
- `ops.html`、`src/entry-ops.js`、`src/ops-app.js`、`src/styles-ops.css` 等内容运营中台文件。
- 32MB 酒店运营规则原始表格和 68MB 拍摄指南附件；现役规则已经整理到 `AI_RULES.md` 与 `skills/`。

## 上传后检查

1. `assets/hotels/` 应包含 924 个文件。
2. `npm test` 应全部通过。
3. AI 规则中心应显示“产品 PRD v1.4”。
4. 正式用户版只应出现“首页、创作、内容、互动、我的”，不应出现独立“发布”“数据”“服务”一级入口。
5. 模型密钥只允许配置在服务器环境变量或未提交的 `.env.local`。
