# 住得满 AI 内容助手双端交付清单

交付日期：2026-07-27

## 本次包含

- 内部工程版：`/`，入口文件为 `index.html`。
- 正式用户版：`/user-a.html`，入口文件为 `user-a.html`。
- Node.js 本地服务、通义千问阶段调用、RAG、领域规则和状态管理。
- 8 个酒店新媒体阶段 Skills 及运行提示词。
- 三个测试账号共 924 张酒店图片，可离线检索、去重和自动编排。
- 当前 PRD v1.4、架构、AI 规则、质量评分、埋点和汇报文档。
- 可直接发送设计师查看的正式用户版单文件原型。
- Node.js 自动化测试。

## 明确不包含

- 内容运营中台：`ops.html` 及 `src/entry-ops.js`、`src/ops-app.js`、`src/styles-ops.css`。
- `.env.local`、真实模型 Key、平台 Token 或其他本地凭证。
- 2.3GB OTA 原始下载缓存；现役代码保留结构化快照和三家测试酒店本地图片。
- 历史用户版、历史原型、旧交付目录和旧压缩包。
- 32MB 原始规则表和 68MB 拍摄视频附件；已整理的有效规则位于 `AI_RULES.md` 和 `skills/`。

## 验收

```bash
npm test
npm run serve
```

- 工程版：`http://localhost:5173/`
- 用户版：`http://localhost:5173/user-a.html`

上传 Git 仓库时请上传解压后的目录内容，不要把 ZIP 本身作为项目源码提交。
