# 项目接手规则

## 项目定位

住得满“AI 内容助手”的可运行本地工程版，包含内部开发界面和正式用户界面，支持酒店选题、内容生成、素材编排、发布补全、互动与数据功能。

## 启动与验证

- Node.js 18+；项目无第三方运行依赖。
- 启动：`npm run serve`
- 测试：`npm test`
- 内部开发版：`http://localhost:5173/`
- 正式用户版：`http://localhost:5173/user-a.html`
- 现役系统入口只有以上两个；`user.html`、`user-v2.html`、`v12.html`、`v123.html` 和单文件原型仅作历史参考，不是验收入口。
- 未经用户明确要求，不主动启动、停止或重启服务。

## 技术栈与目录

- 原生 HTML/CSS/ES Modules；`server.mjs` 提供静态服务与本地 API。
- `src/`：界面、状态、领域规则、RAG 与技能编排。
- `skills/`：八个酒店新媒体运行技能及阶段提示词。
- `tests/`：Node.js 内置测试。
- `assets/hotels/`：三个测试账号可离线检索的酒店图片。

## 现役约定

- `src/`、`server.mjs`、`skills/`、`tests/` 是代码真相源。
- `README.md`、`ARCHITECTURE.md`、`AI_RULES.md` 是运行说明；`住得满AI内容助手_产品需求文档_PRD_v1.4.md` 是当前产品实施基线，但不覆盖代码事实；v1.3 PRD 和旧 HTML 仅作历史输入。
- 正文目标约 120 字，必须在 100-140 字之间。
- 单篇固定 5 图：1 张封面 + 4 张内容图；模型、服务端、前端和发布表单必须一致。
- 素材历史按“当前酒店 + 目标平台”隔离：检查最近 10 篇图片、最近 8 篇封面、最近 30 篇文案和最近 5 篇选题指纹；同篇文件不得重复，可识别时至少覆盖 4 个视觉组，每篇至少包含 2 张最近 10 篇未用图片。
- “换一版”必须更换封面、至少替换 2 张图片、标题相似度低于 65%并更换正文首句；自动修复最多 3 轮，仍失败则阻止进入发布并返回具体补拍建议。
- 正式用户版统一称“AI 内容助手”，一级导航固定为“首页、创作、内容、互动、我的”；“发布”只表示发布信息补全或明确提交动作，“服务”归入“我的”。
- 正式用户版首页以今日主推选题为首屏主任务：只展示 1 张推荐封面，不展示其余 4 张内容图；进入内容编辑后仍必须完整展示并使用 1 封面 + 4 内容图。
- 正式用户版不得展示 Skills、提示词、RAG、事实编号、素材库存字段名或工程评分细项；推荐理由必须转换为“为什么推荐、适合谁、内容怎么讲、素材基础”。
- 模型密钥只能放在 `.env.local` 或后端进程内存，禁止写入前端、文档和提交内容。
- 抖音发布与互动在未获得官方授权前必须明确标注为演示，不得伪造真实成功状态。

## 当前状态与下一步

- `user-a.html` 已由方案 A 原型转为正式系统入口，通过 `src/entry-user.js` 挂载共享状态、领域规则、RAG、Skills 与后端 API；`index.html` 通过 `src/entry-internal.js` 挂载内部工程版。
- 本交付版不包含内容运营中台；`ops.html`、`src/entry-ops.js`、`src/ops-app.js` 和 `src/styles-ops.css` 均不属于双端验收范围。
- 当前开发机已安装 `com.zhudemax.ai-content-assistant` 用户级 LaunchAgent，负责登录自启和异常退出自动拉起；配置源位于 `scripts/com.zhudemax.ai-content-assistant.plist`，状态可用 `npm run service:status` 查看。
- `住得满AI内容助手_正式用户版_设计审阅原型.html` 是由 `npm run build:design-prototype` 生成的单文件设计审阅物，不是第三个系统入口；界面变更后应重新构建，禁止直接手改生成文件。
- 通义千问已接通选题与内容两个模型阶段；发布补全和数据复盘当前由本地确定性流程执行，对应 Skills 是治理及生产适配契约。
- 真实抖音发布、生产数据库、对象存储、任务队列和多租户认证仍未接入。
- 2026-07-18 研发交付目录与压缩包是历史快照；再次对外交付前应从当前根目录重新打包。
