# AI 内容工程质量评分与发布门禁

规则版本：`2026.07.24-v9`

## 1. 评分用途

工程质量分用于判断一条酒店图文内容是否具备进入发布确认页的基本条件。它由服务端在模型返回后重新计算，不直接采用模型自评分。

该分数衡量事实引用、风险、选题上下文、品牌配置、平台字段和素材执行完整度，不代表：

- 抖音流量、播放量或转化率预测。
- 法律合规保证或抖音平台最终审核结果。
- 图片审美、摄影质量或真实住客体验评分。
- 已经真实发布到抖音。

## 2. 总分结构

| 维度 | 满分 | 主要判断内容 |
|---|---:|---|
| 事实证据 `grounding` | 25 | 是否有事实引用，引用能否匹配当前酒店知识库 |
| 合规风险 `compliance` | 20 | 极限词、虚假承诺、易变信息、敏感表达和图片真实性 |
| 选题策略 `strategy` | 15 | 是否保留目标、受众和选题依据/内容角度 |
| 品牌一致 `brand` | 10 | 标题正文是否完整，当前账号是否配置品牌语气 |
| 平台表达 `platform` | 15 | 标签、封面字、钩子、CTA 和正文长度是否完整 |
| 素材执行 `material` | 15 | 五图计划、素材分类和镜头计划是否可执行 |
| 合计 | 100 | 六项由服务端确定性相加 |

## 3. 逐项计分公式

### 3.1 事实证据，满分 25

- 没有 `factReferences`：5 分。
- 有事实引用且全部匹配当前 RAG 事实字段：25 分。
- 每出现 1 个未匹配引用扣 5 分，最低保留 10 分。
- 未匹配引用同时加入问题清单，并触发人工确认。

示例：引用“房型｜OTA”和“地址｜OTA”，两项都能匹配当前酒店知识库，得 25 分；其中一项无法匹配，得 20 分。

### 3.2 合规风险，满分 20

| 风险等级 | 得分 | 处理 |
|---|---:|---|
| `safe` | 20 | 未发现当前规则可识别的问题 |
| `low` | 16 | 存在表达或完整性优化项 |
| `medium` | 12 | 易变信息或证据歧义，需要人工确认 |
| `high` | 0 | 直接阻断，不受总分影响 |

“没有发现已知问题”不等于法律保证。高风险包括事实虚构、绝对化承诺、敏感违规、AI 图冒充实拍和跨账号素材。

### 3.3 选题策略，满分 15

- 同时具备内容目标 `objective`、目标受众 `targetAudience`，以及选题依据 `reason` 或内容角度 `contentAngle`：15 分。
- 上述上下文不完整：9 分。

该项确保内容确实由已确认选题生成，而不是脱离选题重新自由创作。

### 3.4 品牌一致，满分 10

- 标题正文完整，当前账号配置了品牌语气 `settings.voice`：10 分。
- 标题正文完整，但没有品牌语气配置：8 分。
- 标题或正文缺失：3 分；同时会被内容完整性校验拦截。

此处只校验品牌语气配置是否进入生成链路。更细的人工语气审阅仍应由运营人员完成。

### 3.5 平台表达，满分 15

| 字段 | 分值 |
|---|---:|
| 至少 3 个有效标签 | 5；不足时 2 |
| 封面短句 `coverText` | 3 |
| 开头钩子 `hook` | 3 |
| 行动引导 `cta` | 2 |
| 正文已通过 100-140 字服务端门禁 | 2 |

正文不在 100-140 字内时不会进入评分结果，而是要求模型重写；不会截断残句凑长度。

### 3.6 素材执行，满分 15

| 条件 | 分值 |
|---|---:|
| 5 个有效素材 ID，并有对应 5 项图片计划 | 8 |
| 不足或超过 5 个有效素材 ID | 0，并触发素材门禁 |
| 5 个素材 ID，但图片计划不完整 | 2 |
| 没有有效素材 | 0 |
| 存在镜头/画面计划 `shotPlan` | 5 |
| 存在有效素材分类 | 2 |

模型返回的素材 ID 必须来自本轮 Top 12 候选白名单；重复或未召回的 ID 会先被服务端移除，再参与评分。素材分只衡量本篇五图计划是否完整，**不能替代跨历史重复门禁**。

## 4. 发布门禁

发布门禁独立于分数执行，判断顺序如下：

1. 当前酒店、目标平台或图片白名单不匹配：`blocked`。
2. 非固定 5 图、同篇素材/文件重复，或可识别视觉组不足 4 组：`blocked`。
3. 与目标平台最近 10 篇中的任一篇重合 5 张或至少 4 张：`blocked`；重合 3 张先自动换图。
4. 最近 10 篇新增图片少于 2 张、封面命中最近 8 篇，或选题指纹重复：先自动修复；三轮仍不通过则 `blocked` 并返回补拍建议。
5. 标题与最近 30 篇任一标题相似度不低于 80%，正文相似度不低于 85%，或正文相似度不低于 80%且图片重合至少 2 张：`blocked`。
6. 合规风险等级为 `high`：`blocked`，无论分数多少都禁止进入发布提交。
7. 总分低于 70：`revise`，必须修改或重新生成。
8. 总分不低于 70，但存在易变信息、人工确认项或未匹配事实：`needs-confirmation`。
9. 总分不低于 70，且没有待确认项：`ready`，可以进入发布信息确认页。

“换一版”还必须相对当前版本更换封面、至少替换 2 张图片、标题相似度低于 65%并更换正文首句；否则仍保留原版本，不把未变化结果展示为新版本。

`ready` 仅表示可以进入发布补全，不表示已经发布到抖音。当前演示适配器只保存本地记录。

评分解释区间：

- 85-100：结构和证据较完整，推荐进入发布确认。
- 70-84：达到基础门槛，仍应检查提示项。
- 50-69：策略、字段或素材明显不足，需要重写。
- 0-49：不可执行或存在严重问题。

## 5. 100 分示例

一条内容满足以下条件时可得 100 分：

- 事实引用全部匹配当前酒店知识库：25。
- 风险等级为 `safe`：20。
- 选题目标、受众和依据完整：15。
- 标题正文完整且账号配置品牌语气：10。
- 至少 3 个标签，并有封面字、钩子、CTA 和合格正文：15。
- 使用 1 张封面和 4 张内容图，具备完整图片计划、镜头计划和素材分类：15。

## 6. 截图中 99 分的原因

`2026.07.20-v6` 及更早版本将“品牌一致”在标题正文完整时固定为 9/10，因此理论最高分只有 99。页面又只展示事实、合规、策略和素材四项，导致总分无法由可见项解释。

从 `2026.07.20-v7` 开始：

- 品牌语气已配置时可以得到 10/10，100 分可达。
- 素材分必须按完整 5 图计划计算，不再因只有 1 张图和 1 项计划就得到满分。
- 内部开发版评分卡展示全部六个维度，并提供本文档入口。

`2026.07.24-v9` 增加平台级历史去重、多平台版本边界和发布结果状态，不改变本评分公式。

## 7. 代码与规则来源

- 确定性计分与门禁：`server.mjs` 的 `evaluateNormalizedContent`。
- 风险扫描：`src/domain.js` 的 `assessRisk`。
- 内容长度与五图常量：`src/domain.js`。
- 合规 Skill：`skills/hotel-compliance-review/SKILL.md`。
- 审核矩阵：`skills/hotel-compliance-review/references/review-matrix.md`。
- 总体 AI 规则：`AI_RULES.md`。
