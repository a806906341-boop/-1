---
name: douyin-publish-completion
description: 将审核通过的酒店内容、真实图片和商家设置映射为抖音创作者中心发布表单，补齐作品描述、话题、活动、封面、合集、声明、音乐、标签、热点、权限和发布时间，并区分本地模拟与真实开放平台提交。
---

# 抖音发布补全

## 前置条件

读取当前账号、审核结果、正文标签、图片计划和发布适配器状态。存在高风险、跨账号素材、空标题正文或零图片时禁止提交，只允许保存草稿。

## 字段映射

1. 作品描述：正文与标签合并，保留可读换行。
2. 封面：使用视觉技能指定首图；无指定时使用第一张有效实拍图。
3. 官方活动：只有输入明确给出活动 ID 时挂载，否则保持不参与。
4. 合集：按账号配置或内容系列选择，禁止模型虚构合集。
5. 作品声明：AI 示意图、品牌合作、取材来源等按内容事实填写。
6. 音乐：输出音乐氛围建议；没有版权和曲库结果时不指定具体曲名。
7. 内容标签：选择与住宿、旅行和具体场景一致的标签。
8. 关联热点：只有实时接口返回且与内容相关时使用。
9. 发布设置：同步、可见范围、保存权限和立即/定时发布全部显式保存。
10. 地点定位：沿用内容阶段的酒店名称、地址、经纬度和门店 POI 匹配状态；已授权时绑定平台 POI ID，未授权时保留“待抖音门店匹配”，不得虚构已关联。

## 创作者中心界面一致性

发布补全页按抖音创作者中心截图分为“基础信息、扩展信息、发布设置、右侧图片上传区、发布助手”五部分。基础信息必须展示作品标题20字计数、作品描述1000字计数、话题、官方活动、封面、合集和自主声明；扩展信息必须展示音乐、内容标签和关联热点；发布设置必须展示同步发布、可见范围、保存权限和发布时间。图片按视觉技能的顺序自动上传，第一张作为封面并允许切换。每个字段显示自动补全状态与补全依据，缺少实时接口的数据不得伪造成已关联。

## 适配器边界

- `demo`：写入本地发布记录，不声称已发布到抖音。
- `authorized`：提交前生成幂等键，保存平台任务 ID 和原始状态。
- `failed`：保存完整表单、错误码、错误信息和可重试标识。
- 未获得 App ID、OAuth 授权和发布权限时，不得调用或模拟真实成功响应。

字段字典与提交状态见 [references/field-mapping.md](references/field-mapping.md)。
