# 住得满「AI 内容助手」正式用户版
# 初版埋点数据需求文档 v1.0

> 文档状态：M01—M06 已确认，待研发实施  
> 产品阶段：0—1 最小闭环验证  
> 适用入口：`http://localhost:5173/user-a.html`  
> 不适用入口：`http://localhost:5173/` 内部工程版  
> 创建日期：2026-07-24  
> 关联基线：`住得满AI内容助手_产品需求文档_PRD_v1.4.md`

---

## 0. 文档结论

本期不建设完整的数据平台，不追求“所有按钮都有埋点”。只验证一件事：

> **酒店用户能否从首页开始，完成选题、AI 内容生成、内容确认并进入发布提交。**

本期范围固定为：

- 12 个“首页—创作—发布”核心链路事件；
- 3 个一级页面访问事件；
- 1 条核心转化漏斗；
- 1 组 AI 运行结果；
- 1 组桌面端/移动端对比结果。

合计 **15 个事件**。未经产品重新确认，不在本期追加留存、滚动深度、服务销售、互动处理、逐图操作等精细埋点。

---

## 1. 已确认产品决策

| 编号 | 已确认决策 | 本文落实方式 |
|---|---|---|
| M01 | 初版只做 15 个事件 | 事件清单固定为 E01—E15 |
| M02 | 核心只有“首页—创作—发布”一条漏斗 | 以 `task_id` 串联完整创作任务 |
| M03 | “内容、互动、我的”只记录页面访问 | 每个页面仅保留 1 个访问事件 |
| M04 | 不建设复杂用户画像、留存和服务销售分析 | 本文不定义相关事件和指标 |
| M05 | 不采集正文等原始内容 | 只采集 ID、状态、数量、耗时和错误码 |
| M06 | 先确认文档，再开发代码 | 本文是研发实施依据，不代表埋点已经上线 |

### 1.1 与 PRD v1.4 的关系

PRD v1.4 第 13 章描述的是较完整的目标态埋点体系。本文是 0—1 阶段的**最小实施子集**：

- 当前研发和验收优先以本文为准；
- PRD 中互动回复、服务线索、设置反馈等精细事件暂缓；
- 暂缓不等于删除，待核心链路跑通后再评估第二期；
- 本文不改变正式用户版现有产品功能和页面结构。

---

## 2. 本期要回答的问题

埋点上线后，产品每天至少可以准确回答以下六个问题：

1. 有多少家酒店进入正式用户版首页？
2. 有多少家酒店点击首页核心创作按钮？
3. AI 内容生成发起了多少次，成功和失败分别多少次？
4. 用户最常在哪一步停止继续操作？
5. 有多少次创作最终进入并提交发布？
6. 桌面端与移动端的核心链路转化是否存在明显差异？

本期数据不用于回答：

- 用户为什么长期留存或流失；
- 哪种图片操作方式更好；
- 哪类评论更容易转化预订；
- 哪个增值服务入口更容易成交；
- 内容发布后的真实抖音经营归因；
- 多角色、多门店、多渠道用户画像。

---

## 3. 核心业务漏斗

```text
E01 进入首页
  ↓
E02 点击首页核心创作按钮
  ↓
E04 确认选题
  ↓
E05 发起内容生成
  ↓
E06 内容生成成功
  ↓
E09 确认内容
  ↓
E10 进入发布确认页
  ↓
E11 点击提交发布
  ↓
E12 获得发布提交结果
```

### 3.1 首页实际交互的特殊说明

现役首页主按钮为“立即生成这条图文”。用户点击后会同时完成：

1. 确认当前主推选题；
2. 发起内容生成。

因此首页点击一次时，前端可以按顺序记录：

```text
home_create_click
topic_confirm_click
content_generate_start
```

三个事件必须使用同一个 `task_id`，且不能因为一次点击连续上报而合并成一个事件。它们分别代表“入口点击、业务确认、模型请求”三个不同阶段。

从“创作”页面选择选题时，不记录 `home_create_click`，只记录：

```text
topic_confirm_click
content_generate_start
```

---

## 4. 公共字段

所有 15 个事件都必须携带以下公共字段。

| 字段 | 类型 | 必填 | 示例 | 口径 |
|---|---|---:|---|---|
| `event_name` | string | 是 | `user_home_view` | 事件英文名称 |
| `event_time` | string | 是 | `2026-07-24T10:30:15+08:00` | 客户端事件发生时间，ISO 8601 |
| `session_id` | string | 是 | `ses_01K0...` | 一次连续访问会话标识 |
| `hotel_id` | string | 是 | `514254` | 当前酒店账号唯一标识 |
| `device_type` | enum | 是 | `mobile` | 仅允许 `desktop`、`mobile` |
| `page_name` | enum | 是 | `home` | 当前正式用户版页面 |
| `app_version` | string | 是 | `1.4.0` | 当前应用版本 |
| `environment` | enum | 是 | `local_test` | `local_test`、`staging`、`production` |

### 4.1 会话口径

- 首次进入正式用户版时创建 `session_id`；
- 连续 30 分钟无操作后重新创建；
-刷新页面但未超过 30 分钟时沿用；
- 不使用用户输入内容生成会话 ID。

### 4.2 设备口径

- `desktop`：页面真实视口宽度大于 560px；
- `mobile`：页面真实视口宽度小于或等于 560px；
- 内部工程版的“移动预览”不计入正式用户版数据；
- 不根据浏览器 User-Agent 猜测平板或手机品牌。

### 4.3 页面名称

本期只允许以下值：

| 页面 | `page_name` |
|---|---|
| 首页 | `home` |
| 创作－选题 | `create_topic` |
| 创作－编辑 | `create_editor` |
| 创作－发布确认 | `publish_confirm` |
| 内容 | `content` |
| 互动 | `interaction` |
| 我的 | `mine` |

---

## 5. 创作任务关联字段

只在创作链路相关事件中使用。

| 字段 | 类型 | 必填规则 | 说明 |
|---|---|---|---|
| `task_id` | string | E02、E04—E12 必填 | 一次从选题到发布的创作任务 |
| `topic_id` | string | E04—E12 有选题后必填 | 用户确认的选题 |
| `draft_id` | string | E06、E09—E12 生成草稿后必填 | 本次生成的内容草稿 |
| `source` | enum | E02、E04 必填 | 事件入口来源 |
| `result` | enum | 结果事件必填 | `success` 或 `fail` |
| `duration_ms` | number | E06、E07 必填 | 本次模型请求总耗时 |
| `error_code` | string | 失败时必填 | 规范化错误码，不上传完整异常堆栈 |
| `regenerate_count` | number | E06—E09 必填 | 当前任务累计重新生成次数 |
| `publish_mode` | enum | E11、E12 必填 | `now`、`scheduled`、`draft` |
| `adapter_mode` | enum | E12 必填 | `demo` 或 `official` |

### 5.1 `task_id` 生命周期

- 用户从首页点击主按钮时创建；
- 用户从创作页确认一个选题时创建；
- 从生成、编辑到发布提交期间保持不变；
- 重新生成不创建新 `task_id`，只增加 `regenerate_count`；
- 用户放弃当前创作并确认另一个选题时，创建新的 `task_id`；
- `draft_id` 可以在重新生成后变化，但必须保留同一 `task_id`。

### 5.2 `source` 允许值

| 值 | 含义 |
|---|---|
| `home_main_cta` | 首页“立即生成这条图文” |
| `home_continue` | 首页继续编辑或继续发布 |
| `create_topic_card` | 创作页推荐选题卡 |
| `create_custom_topic` | 创作页自定义选题 |
| `editor_regenerate` | 编辑页重新生成 |

---

## 6. 15 个事件总表

| 编号 | 事件名称 | 中文名称 | 触发时机 | 所属范围 |
|---|---|---|---|---|
| E01 | `user_home_view` | 用户版首页访问 | 正式用户版首页成功展示 | 核心漏斗 |
| E02 | `home_create_click` | 首页开始创作点击 | 点击首页核心创作按钮 | 核心漏斗 |
| E03 | `topic_list_view` | 推荐选题页访问 | 创作选题页成功展示 | 辅助诊断 |
| E04 | `topic_confirm_click` | 确认选题 | 用户确认首页或创作页选题 | 核心漏斗 |
| E05 | `content_generate_start` | 内容生成开始 | 内容模型请求发出前 | 核心漏斗 |
| E06 | `content_generate_success` | 内容生成成功 | 服务端返回合格内容并写入草稿 | 核心漏斗 |
| E07 | `content_generate_fail` | 内容生成失败 | 模型、服务端校验或网络导致失败 | 核心诊断 |
| E08 | `content_regenerate_click` | 点击重新生成 | 用户确认重新生成新版本 | 结果采纳 |
| E09 | `content_confirm_click` | 确认内容 | 用户确认当前文案、图片和地点 | 核心漏斗 |
| E10 | `publish_confirm_view` | 发布确认页访问 | 发布确认页面和补全字段成功展示 | 核心漏斗 |
| E11 | `publish_submit_click` | 点击提交发布 | 用户点击立即发布或定时发布 | 核心漏斗 |
| E12 | `publish_submit_result` | 发布提交结果 | 发布适配器返回成功或失败结果 | 核心漏斗 |
| E13 | `content_page_view` | 内容页访问 | 一级“内容”页面成功展示 | 页面访问 |
| E14 | `interaction_page_view` | 互动页访问 | 一级“互动”页面成功展示 | 页面访问 |
| E15 | `mine_page_view` | 我的页访问 | 一级“我的”页面成功展示 | 页面访问 |

---

## 7. 事件详细规格

### 7.1 页面访问事件

| 事件 | 触发条件 | 去重规则 | 额外字段 |
|---|---|---|---|
| `user_home_view` | 路由进入首页且首页主体完成渲染 | 同一 `session_id` 每次进入首页记录 1 次；重复渲染不记录 | 无 |
| `topic_list_view` | 路由进入创作选题页且列表完成渲染 | 每次从其他页面进入记录 1 次 | `recommendation_count` |
| `publish_confirm_view` | 发布确认页完成渲染且存在有效 `publishForm` | 同一 `task_id` 每次进入记录 1 次；组件重绘不记录 | `task_id`、`topic_id`、`draft_id` |
| `content_page_view` | 一级“内容”页成功展示 | 每次从其他一级页面进入记录 1 次 | 无 |
| `interaction_page_view` | 一级“互动”页成功展示 | 每次从其他一级页面进入记录 1 次 | 无 |
| `mine_page_view` | 一级“我的”页成功展示 | 每次从其他一级页面进入记录 1 次 | 无 |

页面访问事件不得在组件局部刷新、账号下拉框重绘、通知弹窗开关时重复上报。

### 7.2 点击与确认事件

| 事件 | 触发条件 | 不触发情况 | 额外字段 |
|---|---|---|---|
| `home_create_click` | 用户点击首页主按钮“立即生成这条图文” | 按钮禁用、程序自动跳转、首页继续编辑 | `task_id`、`topic_id`、`source=home_main_cta` |
| `topic_confirm_click` | 用户明确选择首页主推选题、推荐卡或自定义选题 | 仅查看详情、切换主推卡、模型自动推荐 | `task_id`、`topic_id`、`source` |
| `content_regenerate_click` | 用户在确认弹窗中点击“确认生成新版本” | 只点击编辑页“重新生成”但在弹窗取消 | `task_id`、`topic_id`、`draft_id`、`regenerate_count` |
| `content_confirm_click` | 用户点击“确认内容，去发布补全”或通过人工确认继续 | 按钮禁用、风险阻断、只保存编辑内容 | `task_id`、`topic_id`、`draft_id`、`regenerate_count` |
| `publish_submit_click` | 用户点击立即发布或定时发布提交按钮 | 保存草稿、返回编辑、字段校验未通过 | `task_id`、`draft_id`、`publish_mode` |

点击事件按用户每次有效点击记录。前端防重复提交期间产生的连续点击只记录第一次。

### 7.3 AI 结果事件

| 事件 | 上报位置 | 触发条件 | 额外字段 |
|---|---|---|---|
| `content_generate_start` | 前端 | 正式发出内容生成请求前 | `task_id`、`topic_id`、`source`、`regenerate_count` |
| `content_generate_success` | 后端结果为准 | 模型返回内容，且正文、事实、素材和门禁校验通过，前端获得可编辑草稿 | `task_id`、`topic_id`、`draft_id`、`duration_ms`、`regenerate_count`、`model` |
| `content_generate_fail` | 后端结果为准 | 请求超时、网络失败、模型失败、解析失败或业务校验失败 | `task_id`、`topic_id`、`duration_ms`、`regenerate_count`、`error_code`、`failure_stage` |

同一个模型请求只能产生一个最终结果事件：成功或失败二选一。

### 7.4 发布结果事件

| 事件 | 上报位置 | 触发条件 | 额外字段 |
|---|---|---|---|
| `publish_submit_result` | 后端或发布适配器结果为准 | 发布适配器明确返回成功或失败 | `task_id`、`draft_id`、`publish_mode`、`adapter_mode`、`result`、`error_code` |

说明：

- 当前本地演示适配器必须记录 `adapter_mode=demo`；
- 获得正式抖音授权并接入官方接口后记录 `adapter_mode=official`；
- 演示提交成功与官方发布成功必须通过 `adapter_mode` 区分；
- 不允许前端仅因为按钮点击就直接上报 `result=success`。

---

## 8. 最小错误码

本期只定义足以定位 0—1 链路问题的错误码。

| 错误码 | 含义 | 适用事件 |
|---|---|---|
| `AI_NOT_CONFIGURED` | 模型尚未配置 | `content_generate_fail` |
| `AI_NETWORK_ERROR` | 模型请求网络失败 | `content_generate_fail` |
| `AI_TIMEOUT` | 模型请求超时 | `content_generate_fail` |
| `AI_RESPONSE_INVALID` | 模型响应无法解析或结构不合法 | `content_generate_fail` |
| `CONTENT_VALIDATION_FAIL` | 正文、事实、素材或门禁校验失败 | `content_generate_fail` |
| `PUBLISH_FORM_INVALID` | 发布字段校验失败 | `publish_submit_result` |
| `PUBLISH_ADAPTER_ERROR` | 发布适配器调用失败 | `publish_submit_result` |
| `UNKNOWN_ERROR` | 未归类异常 | 两个结果事件 |

`error_code` 用于聚合统计；完整错误堆栈仅进入服务端日志，不进入用户行为埋点。

---

## 9. 指标口径

### 9.1 默认统计单位

- 产品转化默认按“去重酒店数”统计；
- 页面访问同时保留事件次数和去重酒店数；
- AI 稳定性按“模型请求次数”统计；
- 同一家酒店一天多次创作时，每个 `task_id` 独立计算。

### 9.2 核心漏斗指标

| 指标 | 公式 |
|---|---|
| 首页开始创作率 | 发生 E02 的去重酒店数 ÷ 发生 E01 的去重酒店数 |
| 选题确认率 | 发生 E04 的去重 `task_id` ÷ 发生 E02 或进入 E03 后操作的去重 `task_id` |
| AI 生成成功率 | E06 次数 ÷ E05 次数 |
| 内容确认率 | 发生 E09 的去重 `task_id` ÷ 发生 E06 的去重 `task_id` |
| 发布确认到达率 | 发生 E10 的去重 `task_id` ÷ 发生 E09 的去重 `task_id` |
| 发布提交率 | 发生 E11 的去重 `task_id` ÷ 发生 E10 的去重 `task_id` |
| 发布提交成功率 | E12 中 `result=success` 的次数 ÷ E11 次数 |
| 全链路完成率 | E12 中 `result=success` 的去重 `task_id` ÷ E01 后创建的去重 `task_id` |

### 9.3 AI 运行指标

| 指标 | 公式 |
|---|---|
| AI 请求量 | E05 事件次数 |
| AI 成功量 | E06 事件次数 |
| AI 失败量 | E07 事件次数 |
| AI 平均耗时 | E06、E07 的 `duration_ms` 平均值 |
| 重新生成率 | 发生 E08 的去重 `task_id` ÷ 发生 E06 的去重 `task_id` |
| 首版采纳率 | `regenerate_count=0` 且发生 E09 的任务 ÷ 首次生成成功任务 |

### 9.4 端侧对比

按 `device_type` 分组，只比较：

- 首页开始创作率；
- AI 生成成功率；
- 内容确认率；
- 发布提交率；
- 全链路完成率。

样本量不足时只展示数量，不对桌面端和移动端下优劣结论。

---

## 10. 最小数据输出

本期不要求建设复杂数据看板。研发完成后，提供一张按天汇总表即可。

| 日期 | 活跃酒店 | 首页访问酒店 | 开始创作任务 | 生成成功 | 生成失败 | 确认内容 | 进入发布确认 | 提交发布 | 提交成功 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2026-07-24 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

另附两项辅助统计：

1. 按 `error_code` 汇总的 AI 和发布失败次数；
2. 按 `device_type` 汇总的核心漏斗转化率。

表中没有真实事件时必须显示 0，不使用编造的演示数据填充正式统计。

---

## 11. 前后端职责

### 11.1 前端负责

- 页面成功展示事件；
- 用户有效点击和确认事件；
- 创建并持续传递 `session_id`、`task_id`；
- 采集当前酒店、设备、页面、入口来源；
- 发起 AI 请求前记录 `content_generate_start`；
- 埋点失败不得阻断选题、生成、编辑和发布。

### 11.2 后端负责

- 接收、校验并保存事件；
- 对必填字段和枚举值进行校验；
- 以服务端结果记录 AI 成功、失败和耗时；
- 以发布适配器结果记录提交结果；
- 对重复结果事件去重；
- 不把 API Key、Token、完整异常堆栈写入埋点数据。

### 11.3 初版建议实现方式

为优先跑通 0—1，可先提供统一接口：

```http
POST /api/analytics/events
Content-Type: application/json
```

请求示例：

```json
{
  "event_name": "content_generate_success",
  "event_time": "2026-07-24T10:30:15+08:00",
  "session_id": "ses_01K0ABC",
  "hotel_id": "514254",
  "device_type": "mobile",
  "page_name": "create_editor",
  "app_version": "1.4.0",
  "environment": "local_test",
  "task_id": "task_01K0DEF",
  "topic_id": "topic_01K0GHI",
  "draft_id": "draft_01K0JKL",
  "duration_ms": 6840,
  "regenerate_count": 0,
  "result": "success"
}
```

本地验证阶段可以由后端按行写入 JSONL 文件；未来接入正式分析平台时，只替换保存适配器，不修改页面事件名称和业务口径。

---

## 12. 数据安全

### 12.1 禁止采集

- 模型 API Key、访问 Token；
- 标题和正文原文；
- 模型完整提示词；
- RAG 完整召回内容；
- 评论和私信原文；
- 联系人、手机号、微信号；
- 酒店内部敏感经营数据；
- 完整客户端或服务端异常堆栈。

### 12.2 允许采集

- 酒店、选题、任务和草稿 ID；
- 页面和入口来源；
- 成功、失败状态；
- 规范化错误码；
- 生成耗时；
- 重新生成次数；
- 发布方式；
- 演示或官方适配器模式。

---

## 13. 研发验收清单

### 13.1 数量与范围

- [ ] 正式用户版合计只有 15 个初版事件。
- [ ] 内部工程版不发送正式用户行为事件。
- [ ] “内容、互动、我的”各只有 1 个页面访问事件。
- [ ] 未新增留存、滚动、服务销售和逐图操作埋点。

### 13.2 核心链路

- [ ] 从首页完成一次创作后，E01、E02、E04、E05、E06、E09、E10、E11、E12 顺序完整。
- [ ] 首页主按钮产生的 E02、E04、E05 使用同一个 `task_id`。
- [ ] 从创作页确认选题不会错误记录 E02。
- [ ] 重新生成沿用原 `task_id`，并增加 `regenerate_count`。
- [ ] AI 一次请求只能产生 E06 或 E07 中的一个。
- [ ] 发布点击不能直接冒充发布成功结果。
- [ ] 演示发布结果包含 `adapter_mode=demo`。

### 13.3 去重和稳定性

- [ ] 页面组件重绘不会重复记录页面访问。
- [ ] 按钮防重复提交期间只记录一次有效点击。
- [ ] 刷新页面不会生成重复结果事件。
- [ ] 埋点接口失败不影响主业务功能。
- [ ] 桌面端和移动端均可完整串联同一漏斗。

### 13.4 数据安全

- [ ] 事件数据中不存在正文、提示词、Key、Token、手机号和私信原文。
- [ ] 失败事件只保存规范错误码，不保存完整异常堆栈。
- [ ] `environment` 能区分本地测试、预发布和正式数据。

---

## 14. 本期完成标准

满足以下条件即可判定初版埋点跑通：

1. 15 个事件均可在正式用户版通过人工操作触发；
2. 一次完整创作可以通过 `task_id` 还原完整漏斗；
3. AI 成功、失败和耗时可准确统计；
4. 能定位用户停止在首页、选题、生成、编辑或发布中的哪一步；
5. 能分别计算桌面端和移动端的核心转化；
6. 不采集本文禁止的敏感或原始内容；
7. 业务页面在埋点接口不可用时仍可正常操作。

---

## 15. 后续阶段进入条件

只有初版事件稳定运行、核心链路有真实样本后，才讨论第二期。建议至少满足：

- 连续 7 天无大面积漏报或重复上报；
- 至少积累 30 家酒店或 100 个有效创作任务；
- 产品已经能明确指出第一期数据无法回答的具体问题。

第二期可能考虑互动处理、内容效果、增值服务转化和留存，但不在本文实施范围内。

